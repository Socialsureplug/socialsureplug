<tr>
    <td colspan="{{ $colspan }}">
        <div class="empty">
            <div class="icon">
                <i class="las la-inbox"></i>
            </div>
            <span>{{ $message ?? 'No data found' }}</span>
        </div>
    </td>
</tr>
